<?xml version="1.0" encoding="utf-8" ?>
<ContentView
       x:Class="FitnessActivityApp.Controls.LiveStatsCard"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    
    <Border
        StrokeThickness="1"
        Stroke="#DDDDDD"
        BackgroundColor="#1E1E1E"
        Padding="18"
        StrokeShape="RoundRectangle 20">

        <VerticalStackLayout Spacing="14">

            <Label
                Text="{Binding SelectedActivityType}"
                FontSize="26"
                FontAttributes="Bold"
                TextColor="White" />

            <Grid
                ColumnDefinitions="*,*"
                RowDefinitions="Auto,Auto,Auto"
                ColumnSpacing="14"
                RowSpacing="14">

                <VerticalStackLayout Grid.Row="0" Grid.Column="0">
                    <Label Text="Time" TextColor="#AAAAAA" FontSize="15" />
                    <Label Text="{Binding DurationText}" TextColor="White" FontSize="22" FontAttributes="Bold" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Row="0" Grid.Column="1">
                    <Label Text="Distance" TextColor="#AAAAAA" FontSize="15" />
                    <Label Text="{Binding DistanceText}" TextColor="White" FontSize="22" FontAttributes="Bold" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Row="1" Grid.Column="0">
                    <Label Text="Current Speed" TextColor="#AAAAAA" FontSize="15" />
                    <Label Text="{Binding CurrentSpeedText}" TextColor="White" FontSize="22" FontAttributes="Bold" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Row="1" Grid.Column="1">
                    <Label Text="Average Speed" TextColor="#AAAAAA" FontSize="15" />
                    <Label Text="{Binding AverageSpeedText}" TextColor="White" FontSize="22" FontAttributes="Bold" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Row="2" Grid.Column="0">
                    <Label Text="Steps" TextColor="#AAAAAA" FontSize="15" />
                    <Label Text="{Binding StepsText}" TextColor="White" FontSize="22" FontAttributes="Bold" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Row="2" Grid.Column="1">
                    <Label Text="Calories" TextColor="#AAAAAA" FontSize="15" />
                    <Label Text="{Binding CaloriesText}" TextColor="White" FontSize="22" FontAttributes="Bold" />
                </VerticalStackLayout>

            </Grid>

        </VerticalStackLayout>

    </Border>

</ContentView>